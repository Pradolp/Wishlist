{
  "name": "wishlist",
  "version": "1.0.0",
  "private": true,
  "description": "Lista de desejos pessoal (CRUD completo) com Node.js puro e SQLite embutido",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "start:no-open": "node src/server.js --no-open"
  }
}
